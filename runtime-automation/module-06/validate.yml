- name: Module 6 Validate
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Validate module completion
      validation_check:
        check: true
        msg: "Module 6 validation — replace with your checks"
