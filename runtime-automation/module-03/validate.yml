- name: Module 3 Validate
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Validate module completion
      validation_check:
        check: true
        msg: "Module 3 validation — replace with your checks"
